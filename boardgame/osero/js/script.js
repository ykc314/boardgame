let banArr = new Array(8);
for (let i = 0; 8 > banArr.length; i++) {
  banArr[i] = new Array(8);
}

// HTMLで定義したテーブルを取得
let ban = document.getElementById('ban');

// 取得したテーブルに盤面生成
banNew();

// 盤面を初期化する
banInit();

// クリックした時に実行されるイベント
for (let )

